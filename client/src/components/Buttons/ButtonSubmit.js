import React from "react";
import styled from "@emotion/styled";

const ButtonSubmitForm = styled.button``;

function ButtonSubmit({ text }) {
  return <ButtonSubmitForm>{text}</ButtonSubmitForm>;
}

export default ButtonSubmit;
